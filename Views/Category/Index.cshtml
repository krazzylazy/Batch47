@model List<Category>
<h1>@ViewData["Title"]</h1>

<p>Here you can see your all categories.</p>

<div class="row">
    <div class="col-md-12">
        <a class="btn btn-outline-dark" asp-controller="Category" asp-action="Create">New Category</a>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <partial name="_Notifications" />
    </div>
</div>

<div class="row mt-3">
    <div class="col-md-12">
        <div class="table-responsive">
            <table class="table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Name</th>
                        <th>Display Order</th>
                        <th>Edit</th>
                        <th>Delete</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var i in Model)
                    {
                        <tr>
                            <td>@i.Id</td>
                            <td>@i.Name</td>
                            <td>@i.DisplayOrder</td>
                            <td>
                                <a class="text-info" asp-action="Edit" asp-controller="Category" asp-route-id="@i.Id">Edit</a>
                            </td>
                            <td>
                                <a class="text-danger" asp-action="Delete" asp-controller="Category" asp-route-id="@i.Id">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js" integrity="sha512-VEd+nq25CkR676O+pLBnDW09R7VQX9Mdiij052gVCp5yVH3jGtH70Ho/UUv4mJDsEdTvqRCFZg0NKGiojGnUCw==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
